export const fabricToCustom = (fabricJson) => {
  // TODO: convert Fabric.js JSON to a custom whiteboard format if needed
  return fabricJson;
};

export const customToFabric = (customJson) => {
  // TODO: convert custom whiteboard format back to Fabric.js JSON
  return customJson;
};
